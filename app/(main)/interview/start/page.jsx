import StartInterviewClient from "../components/StartInterviewClient";

export default function StartInterviewPage() {
  // the client component already handles layout and theming
  return <StartInterviewClient />;
}
